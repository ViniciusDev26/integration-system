/** Default session lifetime: 30 days. */
export const DEFAULT_SESSION_TTL_MS = 30 * 24 * 60 * 60 * 1000;

/** Session token size in bytes (256 bits of entropy). */
export const SESSION_TOKEN_BYTES = 32;
