/** Key prefix (pseudo-folder) under which audio objects are stored in R2. */
export const MUSIC_OBJECT_KEY_PREFIX = "musics/";

/** Key prefix under which music cover images (thumbnails) are stored. */
export const MUSIC_THUMBNAIL_KEY_PREFIX = "musics/thumbnails/";
